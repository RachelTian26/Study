今天开始学文件读写。
文件就是硬盘上的一段文字，程序打开它、读出来、改一改、再写回去。
这件事听起来简单，但它是"程序能记住东西"的全部秘密。

在这之前，我写的程序一关就什么都没了。
列表和字典都活在内存里，程序退出，内存就还给系统了。
文件不一样，文件活在硬盘上，断电都还在。

Day 12 会把这件事升级：不是存纯文本，而是存 JSON。
到时候你就能把字典直接写进文件，读回来还是字典。
那就是 calendar-app 里 data.db 干的事，只是它用的是数据库。

先把文件这一关过了。
