"""
Day 14 (08-14)  第 2 周综合项目：通讯录（数据存 JSON）

今天不看新教程。把这一周的东西全用上，做一个**关掉再打开数据还在**的程序。

=================== 为什么是通讯录 ===================

Day 5 和 Day 6 你做过两版通讯录，都有同一个毛病：程序一关，数据全没。
今天补上最后一块 —— 存储。做完这个，你写的程序第一次有了"记忆"。

Day 20 的待办工具，架子跟今天完全一样，只是数据换成事项、多了 AI 解析。


=================== 需求 ===================

一个命令行通讯录，菜单驱动：

  1 添加联系人   姓名 / 电话 / 分组 / 备注（分组和备注可以留空）
  2 查询联系人   按姓名模糊搜，"明" 能搜到 "小明"；也能按分组搜
  3 修改联系人   选中一个，改电话或备注（不想改的字段直接回车跳过）
  4 删除联系人   删之前要确认（输 y 才真删）
  5 显示全部     表格对齐，按姓名排序，末尾显示总人数和分组统计
  6 退出         退出前存盘

数据存在 contacts.json，程序启动时自动读，每次改动后自动存。


=================== 一条联系人长这样 ===================

{
  "name": "小明",
  "phone": "13800138000",
  "group": "同学",
  "note": "同桌",
  "created_at": "2026-08-14T10:30:00"
}


=================== 硬性要求（做完自己逐条打勾）===================

  [ ] 关掉程序再打开，数据还在                    ← 这是唯一的核心指标
  [ ] contacts.json 被删掉后，程序能正常启动（空通讯录开始），不崩
  [ ] contacts.json 内容被改坏（比如手动删掉一个括号），程序给提示不崩
  [ ] 任何菜单项输入乱七八糟的东西都不崩（字母、空回车、超长字符串）
  [ ] 电话号码有校验：必须 11 位数字，不合法要求重输
  [ ] 姓名不能为空，也不能和已有的重名（重名了问要不要改成别的）
  [ ] 至少 8 个函数，main() 里只做流程调度，不写具体逻辑
  [ ] 中文正常显示，不是 \\u5c0f\\u660e
  [ ] 存的时间用 datetime.now().isoformat()


=================== 建议的动手顺序 ===================

千万别想一口气写完。按这个顺序，每步写完立刻运行：

  第 1 步  先把 load / save 跑通。写死两条假数据 → save → 打开 json 文件看一眼
           → 删掉内存里的数据 → load 回来 → print 出来。
           这一步过了，项目的地基就有了。

  第 2 步  写 show_all()，把第 1 步的假数据打印成表格。先不管对齐。

  第 3 步  写 add_contact()，能加一条并存盘。加完重启程序，用 show_all 确认还在。
           ← 到这一步你就已经做出"有记忆的程序"了，剩下的都是加功能。

  第 4 步  写 find_contacts()，模糊搜索。

  第 5 步  写 delete_contact() 和 edit_contact()。

  第 6 步  写 main() 菜单循环，把上面的函数串起来。

  第 7 步  回头做"防崩"：删掉 json 文件试试、把 json 改坏试试、
           菜单输入 "abc" 试试、电话输 "abc" 试试。

  第 8 步  调对齐，让表格好看。（中文宽度的坑见 Day 8）

卡住超过 20 分钟就来问我，带上代码和报错。


=================== 可以直接复用的东西 ===================

Day 13 你写的 mytools.py 里应该已经有 load_json / save_json / safe_int / get_number。
今天直接 import 用，不要重写：

    from mytools import load_json, save_json

如果 Day 13 第 4 题没做完，先花 10 分钟补上，比今天重写一遍划算。
"""

from datetime import datetime
from pathlib import Path

# 数据文件放在这个文件旁边
HERE = Path(__file__).parent
CONTACTS_FILE = HERE / "contacts.json"


# ============================================================
# 下面是骨架。函数签名和说明给好了，实现你来写。
# 顺序是"从底层到上层"，建议也按这个顺序写。
# ============================================================


# ------------------------------------------------------------
# 一、存储层
# ------------------------------------------------------------

def load_contacts():
    """
    从 CONTACTS_FILE 读联系人列表，返回 list。

    三种情况都要处理好：
      文件不存在      → 返回 []（这是正常情况，不是错误！第一次运行就是这样）
      文件内容坏了    → 提示一句然后返回 []，别让程序崩
      读到了          → 返回列表

    提示：Day 12 的 load_json 就是干这个的，直接 import 用。
         自己写也行，记得 except json.JSONDecodeError。
    """
    # TODO


def save_contacts(contacts):
    """
    把联系人列表写回 CONTACTS_FILE。

    记住 Day 12 的三个参数：
        json.dump(contacts, f, ensure_ascii=False, indent=2)
    ensure_ascii=False 不写，文件里中文会变成 \\u5c0f\\u660e。
    """
    # TODO


# ------------------------------------------------------------
# 二、输入校验层
# ------------------------------------------------------------

def ask_name(contacts, allow_empty=False):
    """
    问一个合法的姓名并返回。

    规则：
      去掉首尾空格后不能为空（allow_empty=True 时允许，用于"修改时跳过"）
      不能和 contacts 里已有的姓名重复，重复了提示并重问

    提示：判断重名可以用列表推导式或 any()：
          any(c["name"] == name for c in contacts)
    """
    # TODO


def ask_phone(allow_empty=False):
    """
    问一个合法的手机号并返回。

    规则：11 位纯数字。不合法就说清哪里不合法，然后重问。
         allow_empty=True 时，直接回车返回空字符串（修改时表示"不改"）。

    想一想：这里用 .isdigit() 够吗？
           用户输 "138-0013-8000" 或 " 13800138000 " 你要不要帮他清理一下？
           帮他清理是更好的产品设计。
    """
    # TODO


def confirm(prompt):
    """
    问一个是/否，返回 True / False。

    输 y / Y / yes 都算是，其他都算否。
    删除操作前必须用它。
    """
    # TODO


# ------------------------------------------------------------
# 三、业务逻辑层
# ------------------------------------------------------------

def add_contact(contacts):
    """
    添加一个联系人。

    流程：问姓名 → 问电话 → 问分组（可空）→ 问备注（可空）
         → 组成 dict（带 created_at）→ append 进 contacts → 存盘 → 提示成功

    created_at 用 datetime.now().isoformat()

    注意：这个函数改了 contacts（append），列表是可变对象，
         改动在函数外面也能看到 —— 这点跟 Day 6 学的作用域不冲突，
         因为你改的是列表内容，不是重新赋值。
         （如果你写 contacts = [...] 重新赋值，外面就看不到了，想一想为什么）
    """
    # TODO


def find_contacts(contacts, keyword):
    """
    模糊搜索，返回匹配的联系人列表（可能是空列表）。

    要求：姓名、分组、备注里任何一个包含 keyword 就算匹配。
         不区分大小写。

    注意这个函数只负责"找"，不负责"打印" —— 打印是 show_table 的事。
    一个函数只做一件事，这是 Day 6 学的，今天要真的做到。
    """
    # TODO


def pick_one(contacts, action_name):
    """
    让用户选中一个联系人，返回那个 dict（取消或找不到返回 None）。

    修改和删除都要先"选中一个"，所以抽出来复用。

    流程：问关键词 → find_contacts →
         0 个：提示没找到，返回 None
         1 个：直接返回它
         多个：列出来编号让用户选，选错了或输空就返回 None

    action_name 是 "修改" / "删除" 这样的词，用来拼提示语。
    """
    # TODO


def edit_contact(contacts):
    """
    修改一个联系人的电话 / 分组 / 备注。

    流程：pick_one 选中 → 显示当前信息 → 逐个字段问新值
         → 直接回车表示不改这个字段 → 存盘

    提示：显示当前值让用户有参照，体验会好很多：
         new_phone = ask_phone(allow_empty=True)  # 提示语里带上当前电话
    """
    # TODO


def delete_contact(contacts):
    """
    删除一个联系人。

    流程：pick_one 选中 → 显示信息 → confirm 确认 → contacts.remove(它) → 存盘

    必须确认。这是今天唯一一个不可逆操作。
    """
    # TODO


# ------------------------------------------------------------
# 四、显示层
# ------------------------------------------------------------

def show_table(contacts, title="全部联系人"):
    """
    打印联系人表格。

    要求：表头 + 分隔线 + 每行一个联系人 + 末尾人数
         按姓名排序（提示：sorted(contacts, key=lambda c: c["name"])）
         列表为空时打印"通讯录是空的"，不要打印一个光秃秃的表头

    对齐用 Day 8 学的 f"{值:<8}"。中文宽度会歪，见 Day 8 的说明，
    最省事的办法是让每列的中文字数尽量一致。
    """
    # TODO


def show_stats(contacts):
    """
    打印统计：总人数、每个分组几个人、最早添加的是谁。

    Day 7 的 print_summary 你没写完，这次补上。
    提示：分组统计用 Day 12 的 setdefault 或 Day 13 的 Counter，
         最早添加的可以用 min(contacts, key=lambda c: c["created_at"])
         （created_at 是 ISO 格式字符串，字符串比大小刚好就是时间顺序，很方便）
    """
    # TODO


# ------------------------------------------------------------
# 五、主流程
# ------------------------------------------------------------

def show_menu():
    """打印菜单。单独抽出来，因为循环里每轮都要打一次。"""
    # TODO


def main():
    """
    主流程：

      1. contacts = load_contacts()
      2. 打个招呼，说明当前有几个联系人
      3. while True 循环：
           打印菜单 → 读选项 → 按选项调对应函数
           6 就存盘 + 道别 + break
           输入不认识的选项 → 提示后继续循环，不能崩
      4. 整个循环外面套一层 try/except KeyboardInterrupt，
         用户按 Ctrl+C 时存盘并友好退出，而不是吐红字

    注意：main() 里只有"调度"，没有任何具体逻辑。
         如果你发现 main() 里出现了 for 循环或者字符串拼接，
         那说明有东西该抽成函数了。
    """
    # TODO


if __name__ == "__main__":
    main()


# ============================================================
# 做完之后的验收清单
# ============================================================
#
# 功能测试：
#   1. 加 3 个联系人 → 退出程序 → 重新运行 → 显示全部，3 个人都在
#   2. 打开 contacts.json 看一眼，中文是中文，格式是缩进过的
#   3. 搜一个只输一个字的关键词，能模糊搜到
#   4. 改一个人的电话，重启确认改动存下来了
#   5. 删一个人，先输 n 确认没删掉，再输 y 确认删掉了
#
# 抗崩测试（这一步才是第 2 周真正的考试）：
#   6. 删掉 contacts.json，运行程序 → 应该正常启动，空通讯录
#   7. 手动打开 contacts.json，删掉最后一个 ]，运行 → 应该提示格式坏了，不崩
#   8. 菜单输入 "abc"、直接回车、输 "99" → 都应该提示后继续
#   9. 电话输 "abc"、输 "138"、输 "1380013800012345" → 都应该要求重输
#  10. 姓名直接回车 → 应该要求重输
#  11. 加一个已有的姓名 → 应该提示重名
#  12. 运行中按 Ctrl+C → 应该友好退出并存盘
#
# 代码质量自查：
#  13. 数一下有几个函数？main() 有多少行？（超过 30 行就该再拆）
#  14. 有没有哪段逻辑写了两遍？（写了两遍就该抽成函数，Day 6 的标准）
#  15. 有没有裸 except: ？（有就改掉，Day 11 讲过为什么）
#
# 全过了，第 2 周就真的完成了。
# 然后做两件事：
#   • 写 day14_summary.md（模板见 python/SUMMARY_TEMPLATE.md），"卡在哪"那段必填
#   • 让我 review 你的代码。你写完之后我给意见，这个顺序不能反。
