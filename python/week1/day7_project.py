"""
Day 7 (08-07)  第 1 周综合项目：成绩统计器

今天不看新教程。目标是把前 6 天学的东西全部用上，做出一个完整能跑的程序。

=================== 需求 ===================

做一个命令行成绩统计器，功能：

  1. 录入成绩：输入学生姓名，然后依次输入语文/数学/英语三科分数
  2. 可以连续录入多个学生，输入空名字表示录完了
  3. 录完后输出一张成绩表，包含：姓名、三科分数、总分、平均分、等级
  4. 表格下面输出全班统计：人数、各科平均分、总分最高的学生
  5. 分数输入非法（不是数字，或不在 0-100）时要提示重输，不能崩

=================== 期望输出长这样 ===================

姓名      语文   数学   英语   总分   平均   等级
--------------------------------------------------
小明       90     85     78    253   84.3    B
小红       88     92     95    275   91.7    A
--------------------------------------------------
共 2 人
各科平均：语文 89.0  数学 88.5  英语 86.5
总分最高：小红 (275)

=================== 要求 ===================

必须用到（自己检查）：
  [ ] 函数：至少拆出 4 个函数，main() 里只做流程调度
  [ ] 字典：每个学生用一个 dict 存
  [ ] 列表：所有学生放在一个 list 里
  [ ] 循环：录入用 while，输出用 for
  [ ] 条件判断：等级评定
  [ ] f-string 对齐：用 {值:>6} 这类写法把表格对齐

=================== 建议的动手顺序 ===================

别想一口气写完。按这个顺序，每步都运行一次：

  第 1 步：先写 get_score() —— 只做"输入一个合法分数"这一件事，反复测试
  第 2 步：写 input_one_student() —— 输入一个学生，返回一个 dict
  第 3 步：写主循环，能连续录入多个学生，先只 print(students) 看数据对不对
  第 4 步：写 print_table() —— 表头和表身，先不管对齐，能出来就行
  第 5 步：调对齐，把表格调好看
  第 6 步：写 print_summary() —— 全班统计
  第 7 步：回头补 grade() 等级评定

卡住超过 20 分钟就来问我，带上代码和报错。
"""


# ============================================================
# 下面是骨架。函数签名和说明已经给好，你来填实现。
# ============================================================


def get_score(subject):
    """
    让用户输入一科的分数，直到输入合法为止，返回 int。

    合法 = 能转成数字 且 在 0-100 之间。
    不合法就提示一句然后再问一次。

    提示：用 while True 配合 return —— 合法就 return，不合法就 continue。
         判断字符串是不是纯数字可以用 .isdigit()
    """
    # TODO
    pass


def input_one_student():
    """
    录入一个学生，返回字典，格式：
        {"name": "小明", "chinese": 90, "math": 85, "english": 78}

    如果用户名字输入为空（直接回车），返回 None，表示录入结束。

    提示：三科分数调用 get_score() 来拿，别重复写输入逻辑。
    """
    # TODO
    pass


def total(student):
    """返回这个学生的总分。"""
    # TODO
    pass


def average(student):
    """返回这个学生的平均分（float，不用管小数位数，输出时再控制）。"""
    # TODO
    pass


def grade(avg):
    """
    根据平均分返回等级字符串：
        90 及以上 A / 80-89 B / 70-79 C / 60-69 D / 60 以下 F
    """
    # TODO
    pass


def print_table(students):
    """
    打印成绩表：表头 + 分隔线 + 每个学生一行。

    提示：对齐用 f-string 的宽度控制
        f"{name:<8}" 左对齐占 8 格
        f"{score:>5}" 右对齐占 5 格
        f"{avg:>6.1f}" 右对齐占 6 格保留 1 位小数
    """
    # TODO
    pass


def print_summary(students):
    """
    打印全班统计：总人数、各科平均分、总分最高的学生。

    提示：找最高分的学生，可以先假设第一个是最高，然后遍历比较替换。
         列表为空时要注意别报错。
    """
    # TODO
    pass


def main():
    """
    主流程：
      1. 建一个空列表 students
      2. while 循环调 input_one_student()，返回 None 就 break，否则 append
      3. 如果一个人都没录，提示"没有数据"然后结束
      4. 调 print_table() 和 print_summary()
    """
    # TODO
    pass


# 这行的意思：只有直接运行这个文件时才执行 main()
# 如果这个文件被别的文件 import，main() 不会自动跑
# （Day 13 学模块的时候会讲清楚，现在照着写就行）
if __name__ == "__main__":
    main()


# ============================================================
# 做完之后
# ============================================================
# 1. 完整跑一遍，录 3 个学生，检查数字算得对不对（拿计算器验一下）
# 2. 故意输入错误：输入 "abc"、输入 200、输入负数，看程序会不会崩
# 3. 一个学生都不录直接回车，看会不会崩
# 4. 回到上面的「要求」清单，逐条打勾
# 5. 在 diary.md 记一笔：这周哪个概念最难？
#
# 都过了，第 1 周就算真的完成了。可以让我 review 你的代码，我会给改进意见。
