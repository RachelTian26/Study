# 第 2 周（Day 8-14）：能做出东西

目标：**能写出一个存得住数据、不会一崩就退出的命令行工具。**

第 1 周你写的程序，关掉就什么都没了。这周结束时，你的程序会有记忆。

---

## 先说第 1 周

7 天全做完了，代码是你写的，这是最重要的。几个我看到的点：

**做得好的**
- `counts[word] = counts.get(word, 0) + 1` 这种写法你已经会主动用了，这不是初学者的写法
- Day 6、Day 7 的函数拆分是真的懂了 —— `input_one_student()` 里调 `get_score()` 而不是重复写输入逻辑，这个判断很对
- Day 4 第 2 题手写 max/min 时用了 `float("-inf")` 做初始值，这个想法很干净
- 自检题你是真的在答，不是空着（Day 3、Day 4 那几行）

**这周会专门补的**
1. **列表推导式还不是第一反应。** Day 4 第 4 题明明要求用推导式，你写的是 for + append。两种都对，但推导式要练到顺手 —— 这周处理文件和 JSON 时，数据清洗几乎全靠它。
2. **`.isdigit()` 挡不住的东西。** Day 7 用它验证分数，`"12.5"` 和 `"-3"` 都会被判成非法。Day 11 会给你更好的工具。
3. **`print` 和 `return` 混着用。** Day 6 第 5 题 `fizzbuzz(n)` 里面 print，外面又 `print(fizzbuzz(n))`，所以末尾多了个 `None`。想清楚一个函数是"负责算"还是"负责显示"。
4. **别用内置名当变量名。** Day 3 你写了 `sum = 0`，Day 4 写了 `list = [...]`。能跑，但那一行之后 `sum()` 和 `list()` 就用不了了。这类 bug 很隐蔽。
5. **Day 5 第 2 题有个真 bug。** 最后一行输出"最低科目"用的是循环结束后残留的 `subject`，不是真的最低那科；`max(scores.values())` 也在循环里被反复算。这周会有类似的题让你重做一遍。
6. **Day 7 的 `print_summary` 没写。** 需求要的是全班统计（人数、各科平均、总分最高），你写的是又循环打印了一遍每个人。这周 Day 10、Day 12、Day 14 都会让你补上这件事。
7. **Day 7 有个逻辑小坑。** `if name == "" or len(name) > 10: return None` —— 名字太长被当成"录入结束"了，这两件事应该分开处理。

难度按计划加了一点。第 1 周很顺，所以这周每天多 1-2 道题，挑战题也更硬。做不完就先做前几题，规则没变：**宁可少写不要不写。**

---

## 这周的新东西

| 概念 | 你已有的参照 | 新在哪 |
|---|---|---|
| 字符串方法 | JS 的 `split/join/trim` | 名字不同，而且**返回新字符串，不改原来的** |
| 文件读写 | 你没在 JS 里直接写过 | `with open()` 是 Python 特色，路径问题是最大的坑 |
| try/except | JS 的 `try/catch` | 几乎一样，但 Python 讲究"接什么错" |
| JSON | 你太熟了 | 只是换成 `json.load/dump`，注意 `ensure_ascii=False` |
| 模块 / venv | `import` / `node_modules` / `npm` | 对应关系很干净，`__name__` 是唯一的新概念 |

---

## 日程

| Day | 日期 | 学什么 | 文件 | 完成标准 |
|---|---|---|---|---|
| 8 | 08-08 六 | 字符串处理 + 格式化 | `day8_string.py` | `split/join/strip/replace` 用熟 |
| 9 | 08-09 日 | 轻松日：自测 + Codewars | `day9_review.py` | 10 道自测 + 5 道 8kyu |
| 10 | 08-10 一 | 文件读写 | `day10_file.py` | 会用 `with open()`，知道 `r/w/a` 区别 |
| 11 | 08-11 二 | 异常处理 | `day11_error.py` | 知道该捕获什么、**不该**捕获什么 |
| 12 | 08-12 三 | JSON 读写 | `day12_json.py` | dict ↔ JSON 文件互转 |
| 13 | 08-13 四 | 模块 + venv + pip | `day13_module.py`、`textstats.py` | 能建虚拟环境、写自己的模块并 import |
| 14 | 08-14 五 | **周综合：通讯录** | `day14_project.py` | 关掉程序再打开，数据还在 |

---

## Day 9 怎么过

轻松日，不学新语法 —— **但不是可选日，四段都要做完。**
"轻松"指的是脑子不用装新东西，不是指今天可以跳过。`day9_review.py` 分四段，2 小时：

| 时间 | 做什么 |
|---|---|
| 25 分钟 | **Part 0 从零重写** ★ 新建空文件，不看 Day 7 的代码，重写 3 个函数 |
| 25 分钟 | **Part 1 自测** —— 关掉所有文件默写 10 道题，每题标「顺 / 卡 / 漏」 |
| 50 分钟 | **Part 2 Codewars** —— 注册，做 5 道 8kyu |
| 20 分钟 | **Part 3 抄回来** —— 你的解法和最高赞解法并排记下来 |

**Part 0 是今天最有用的一段**，也是我新加的。挑的三个函数各有原因：

- `get_score()` —— 需求改了，这次要接受 `87.5`、拒绝 `-3`，你原来的 `.isdigit()` 两件事都做不到。绕不过去就把问题记下来，Day 11 给你正确工具
- `grade()` —— 纯 `elif` 链，应该 2 分钟写完。写不出来说明还不熟
- `print_summary()` —— **Day 7 这个你没写对**，写成了又打印一遍每个人。这次严格按需求做全班统计，并且空列表不能崩

跟着提示写出来，和自己从零写出来，是两回事。隔一周重写才知道哪些真进了脑子。Day 30 会整套再来一次，今天先小规模练。

自测那 10 道是从第 1 周到 Day 8 挑的。第 10 题是找 bug，三个 bug 都是你第 1 周真实犯过的。

Codewars 的价值是**它会给你看别人的解法** —— 提交通过后能看到同一题最高赞的写法，经常是你写 8 行的东西人家 1 行。这是最快的"手感"来源，Part 3 别省。

时间实在不够（比如只有 40 分钟），按这个优先级砍：Part 0 必做 → Part 1 必做 → Part 2 减到 2 道 → Part 3 只记 1 道。

**交付物**：`week2/day9_summary.md`（四行，格式在文件末尾）+ 一次 git commit。

---

## 文件说明

```
week2/
├── README.md              这个文件
├── day8_string.py         字符串
├── day9_review.py         轻松日：自测 + Codewars 记录
├── day10_file.py          文件读写
├── day11_error.py         异常处理
├── day12_json.py          JSON
├── day13_module.py        模块 / venv / pip
├── day14_project.py       周综合项目（骨架，你来填）
├── textstats.py           我写的示范模块，Day 13 读它 + import 它
├── mytools.py             ← Day 13 第 4 题你自己建，之后一直用
└── data/                  练习用的数据文件，别改原文件
    ├── article.txt        英文短文，词频统计用
    ├── notes.txt          中文短文，按行读取用
    ├── scores.csv         成绩数据，**故意是脏的**（空行/空格/缺列/非数字）
    └── events.json        事项数据，结构照 calendar-app 的 events 表
```

`data/scores.csv` 那些"错误"是我故意留的。Day 10 你会看见它们，Day 11 你会学会处理它们。

练习会生成 `output.txt`、`log.txt`、`contacts.json` 之类的文件，都是产物，`.gitignore` 已经排除了，不用管。

---

## 新增：linter（Day 9 顺手做，10 分钟）

上面那 7 条改进建议里，有 3 条机器能自动帮你抓：内置名当变量名、裸 `except:`、import 了没用。

**ruff 已经装好了（0.16.2），配置也写在 `python/ruff.toml` 了，不用动。** 你只要在 VS Code 里装 **Ruff** 扩展（发布者 Astral Software），它就会在你写代码时直接画波浪线。

手动跑一遍看它抓到什么：

```bash
cd /Users/tian/Downloads/Study/python
python3 -m ruff check week1/
```

我已经跑过了，你第 1 周有 **8 处**：

| 规则 | 数量 | 是什么 |
|---|---|---|
| `A001` | 3 | `sum = 0`（day3 两处）、`list = [...]`（day4 一处）—— 内置名被覆盖 |
| `W291` / `W293` | 5 | 行尾多余空格 |

后 5 个是纯格式问题，一条命令自动修：

```bash
python3 -m ruff check week1/ --fix
```

**前 3 个 `A001` 不要用 `--fix`，自己改。** 该叫 `total` 还是 `nums` 得你自己想，顺手记住为什么不能叫 `sum`。

**注意：linter 只管"写法"，不管"对不对"。** 它抓不出 Day 5 第 2 题那种逻辑 bug，也抓不出 `print_summary` 没按需求写。那些还得靠你自己测和我 review。别因为它没报错就以为代码是对的。

---

## 这周的三个坑，先打个预防针

**1. 路径找不到（`FileNotFoundError`）**

`open("data/notes.txt")` 里的路径不是相对于 `.py` 文件，是相对于你运行命令时所在的目录。
所以每个文件开头都写这两行，以后照抄：

```python
from pathlib import Path
HERE = Path(__file__).parent
DATA = HERE / "data"
```

**2. 中文乱码**

`open()` 一律带 `encoding="utf-8"`，`json.dump()` 一律带 `ensure_ascii=False`。
不带不会立刻报错，但文件打开一看是 `小明`，或者在别人电脑上直接崩。

**3. `except:` 什么都接**

这是这周最容易养成的坏习惯。它能让报错消失，也能让 bug 藏起来。
Day 11 会讲清楚为什么，先记住：**看到裸 `except:` 就是要改的**。

---

## Day 14 结束后自测

- [ ] 程序退出后数据存下来了，重启能读回来
- [ ] 用户输入乱七八糟的东西，程序不崩
- [ ] 数据文件被删掉/改坏，程序不崩
- [ ] 代码拆成了 8 个以上函数，`main()` 里只做调度

前三条是这周的核心。第四条是第 1 周就该有的习惯，这周继续保持。

做完让我 review —— **你写完之后**，这个顺序不能反。
